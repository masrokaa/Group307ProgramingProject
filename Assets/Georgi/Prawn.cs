using UnityEngine;
using System.Collections;

public class Prawn {

    

    /*public static void homeToActive()
    {
        if (Dice.diceroll == 6)
        {
            kordinator[1,1]
        }
    }

    /*
    public static string color;
    public static bool home;
    public static string homePosition;
    public static string startPosition;
    
    

   /* Pawn (string color){
        string currentPosition = this.currentPosition;
        bool home = this.home;
    }

   /* public static void colorPosition() {
    if (Pawn.color == "blue"){
            string homePosition = 
        }

        if (Pawn.color == "green")
        {
            string homePosition =
            }

        if (Pawn.color == "red")
        {
            string homePosition =
        }

        if (Pawn.color == "yellow")
        {
            string homePosition =
        }
    } */


    /*
        public static void movePawn() {
            currentPosition = currentPosition + rndDice;

        }

        public static void killPawn() {
            Pawn.currentPosition = Pawn.homePosition;
        }
        */

    // Use this for initialization
    void start () {
	//pawn class
    //Position - Base or active
    //direction if land on another brick = kills the brick/back to start
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
