using UnityEngine;
using System.Collections;

public class Pawn : MonoBehaviour {

    //public string[] greenPawns = new string[] { "green1", "green2", "green3", "green4"};
    bool home;
    float homePosition;
    float startPosition;
    float currentPosition;
    public string pawnColor;

   /* public void colorHomePosition()
    {
        for (int i = 0; i < greenPawns.Length; i++)
        {
            greenPawns[0] = 
        }

        if ( == 0)
        {
            float homePosition = kordinat[0];
        }

        if (Pawn.color == "red")
        {
            float homePosition = kordinat[10];
        }

        if (Pawn.color == "blue")
        {
            float homePosition = kordinat[20];
        }

        if (Pawn.color == "yellow")
        {
            float homePosition = kordinat[30];
        } 
    }

   static void homeToActive()
    {
        if (Dice.rndDice == 6)
            Pawn.home = false;
            Pawn.homePosition = Pawn.startPosition;
            //roll dice
    }

    public static void movePawn()
    {
        currentPosition = currentPosition + rndDice;
    }

    public static void killPawn()
    {
        Pawn.homePosition = Pawn.currentPosition;
    } */


    // Use this for initialization
    void start()
    {
      /*  Pawn[] greenPawns;
        greenPawns = new Pawn[3];
        Pawn[] redPawns;
        redPawns = new Pawn[3];
        Pawn[] bluePawns;
        bluePawns = new Pawn[3];
        Pawn[] yellowPawns;
        yellowPawns = new Pawn[3]; */
    }

    // Update is called once per frame
    void Update()
    {

    }
}
